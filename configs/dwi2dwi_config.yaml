training:
  mode: dwi2dwi
  data_dir: C:/projetos/Datasets/IXI-DTI
  batch_size: 4
  epochs: 50
  slice_axis: 2

loss:
  components: [l1, ssim]
  weights: [1.0, 0.1]

model:
  name: drunet
  in_channels: 1
  out_channels: 1
